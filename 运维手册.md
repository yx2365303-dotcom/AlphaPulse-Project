git --version：检查本机是否已安装 Git 以及版本号。

• cd "c:\Users\Lenovo\Documents\Obsidian Vault\AlphaPulse Project"：切换到你的 Obsidian 仓库根目录（后续 git 操作都在这里执行）。

• git rev-parse --is-inside-work-tree：判断当前目录是否已是 Git 仓库（有无 .git）。
• git status --porcelain=v1 --untracked-files=all：以“机器可读”方式列出工作区状态（包含未跟踪文件），便于快速确认将要提交什么。

• git init：在当前目录初始化一个新的 Git 仓库（创建 .git）。

• git branch -M main：把当前分支强制命名为 main（兼容不同默认分支名）。

• git remote add origin https://github.com/yx2365303/yx2365303-dotcom.git：添加远端仓库地址为 origin（后续 push/pull 默认用它）。

• git remote -v：查看当前配置的远端地址（fetch/push）。

• git check-ignore -v [.env](http://_vscodecontentref_/2)"：验证某个文件是否被 .gitignore 忽略，并显示命中哪条规则。

• git add -A：把所有变更（新增/修改/删除）加入暂存区，准备提交。

• git commit -m "Initial commit"：创建一次提交，提交信息为 Initial commit。

• git push -u origin main：推送本地 main 到远端 origin，并设置上游跟踪（以后可直接 git push）。

• git config user.name "yx2365303"：仅对当前仓库设置提交用户名（不是全局）。

• git config user.email "yx2365303@users.noreply.github.com"：仅对当前仓库设置提交邮箱（GitHub 常用 no-reply 邮箱）。

• gh --version：检查是否安装 GitHub CLI（gh）及版本。

• gh auth status：检查 gh 是否已登录、使用哪个账号、token 权限范围。

• $env:GH_PAGER="cat"：让 gh 不走分页器（避免输出被分页卡住），常用于命令前临时设置。

• gh repo view yx2365303/yx2365303-dotcom --json name,owner,url --jq "."：查询某个仓库是否存在/可访问，并用 JSON 输出关键信息。

• gh repo view yx2365303-dotcom/yx2365303-dotcom --json name,owner,url,visibility --jq "."：同上，另带可见性字段。

• git remote remove origin：删除现有 origin（我们当时要改用新创建仓库的远端）。

• gh repo create yx2365303-dotcom --private --source . --remote origin --push：在 GitHub 创建仓库（私有），以当前目录为源，自动添加远端为 origin并推送。

• gh repo rename -R yx2365303-dotcom/yx2365303-dotcom AlphaPulse-Project -y：将指定仓库重命名为 AlphaPulse-Project（-y 跳过确认）。

• gh repo edit yx2365303-dotcom/AlphaPulse-Project --visibility public --accept-visibility-change-consequences：将仓库改为 Public（必须显式接受可见性变更后果）。

• git remote set-url origin https://github.com/yx2365303-dotcom/AlphaPulse-Project.git：更新本地 origin 指向重命名后的新地址。

• git ls-files | findstr /i "\.env$"：检查当前已被 Git 跟踪的文件列表里，是否有以 .env 结尾的文件（应当为空，确保密钥没被提交）。


### 1. **快速测试单个功能**

cd "c:\Users\Lenovo\Documents\Obsidian Vault\AlphaPulse Project\tushare_program"

# 测试个股日线
python main.py --daily

# 测试龙虎榜/涨停等日度数据
python main.py --concepts

# 测试概念列表等元数据
python main.py --meta

# 测试热门板块详细数据
python main.py --hot --top-n 10

### 2. **全量测试（所有接口）**
# 不加参数即为全量模式
python main.py

### 3. **使用历史日期测试**
# 临时设置环境变量（Windows PowerShell）
$env:TRADE_DATE="20250110"
python main.py --concepts

# 或者修改 .env 文件
# TRADE_DATE=20250110


### 4. **查看详细日志**
# 修改 .env 文件
# LOG_LEVEL=DEBUG

# 然后运行
python main.py --concepts


### 5. **测试单个接口（高级）**

创建测试脚本 `test_single.py`：

import sys
sys.path.insert(0, '.')

from utils import patch_ssl_and_timeout
patch_ssl_and_timeout()

from core import DataFetcher

# 测试单个接口
fetcher = DataFetcher()
result = fetcher.fetch_and_save(
    'limit_step',           # API 方法名
    'limit_step',           # 表名
    trade_date='20250110'   # 参数
)

print(f"结果: {'成功' if result else '失败'}")


然后运行：

python test_single.py

## 📋 常用测试场景

### 场景 1：验证所有表有数据

# 1. 先执行 supabase_fix.sql 修复数据库
# 2. 运行全量测试
python main.py

# 3. 查看日志，应该看到类似：
# [成功] 写入 44 条到 limit_list_ths
# [成功] 写入 114 条到 limit_list_d
# ...
# 总成功: 10, 总失败: 0


### 场景 2：调试某个失败的接口

# 假设 top_list 失败了，单独测试
python -c "
import sys
sys.path.insert(0, '.')
from utils import patch_ssl_and_timeout
patch_ssl_and_timeout()
from core import DataFetcher
fetcher = DataFetcher()
fetcher.fetch_and_save('top_list', 'top_list', trade_date='20250110')
"

### 场景 3：快速验证数据清洗逻辑

# 使用 simple_run.py（我之前创建的简化脚本）
python simple_run.py

## 🔍 项目目录结构速查

tushare_program/
│
├── main.py                  # ⭐ 主入口（你运行这个）
├── run.py                   # 同 main.py（备份）
│
├── config/                  # 配置模块
│   ├── __init__.py
│   └── settings.py          # 从 .env 加载配置
│
├── core/                    # 核心业务
│   ├── __init__.py
│   ├── client.py            # Tushare & Supabase 客户端
│   └── fetcher.py           # 数据抓取核心逻辑
│
├── tasks/                   # 任务编排
│   ├── __init__.py
│   ├── daily_tasks.py       # 日度数据任务（龙虎榜/涨停等）
│   └── meta_tasks.py        # 元数据任务（概念列表等）
│
├── utils/                   # 工具模块
│   ├── __init__.py
│   ├── network.py           # 网络修复（SSL/超时）
│   ├── logger.py            # 日志工具
│   └── cleaner.py           # 数据清洗（NaN处理）
│
├── .env                     # 环境变量（你的密钥）
├── .env.example             # 环境变量模板
├── requirements.txt         # 依赖包
├── supabase_fix.sql         # 数据库修复SQL
│
└── [历史备份]
    ├── main.py.bak          # 旧版个股日线
    ├── main1.py.bak         # 旧版多接口
    └── README_OLD.md        # 旧版文档

## ✅ 快速验证流程

# 1. 进入项目目录
cd "c:\Users\Lenovo\Documents\Obsidian Vault\AlphaPulse Project\tushare_program"

# 2. 查看帮助
python main.py --help

# 3. 测试单个模块（最快）
python main.py --concepts

# 4. 看到日志输出：
# [2026-01-16 xx:xx:xx] INFO - [limit_step] 开始抓取
# [2026-01-16 xx:xx:xx] INFO -   [成功] 写入 15 条到 limit_step


